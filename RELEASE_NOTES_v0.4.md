# 富丽 v0.4 版本说明

## 版本主题

> **体验收敛**

v0.3 解决：富丽应该如何判断。
v0.4 解决：用户应该如何自然地使用富丽。

## 主要变化

1. **用户入口简化为两个**："富丽扫描" 和 "富丽升级"，去掉 SCAN/REVIEW/REFLECT 的概念负担
2. **统一扫描流程**：宿主 Agent 基础检查 + 富丽创作判断 → 统一 Suggestions
3. **建议标注来源**：`🤖 来自 [宿主名称]` / `🌿 来自富丽`
4. **新增 `富丽升级`**：一键检查并安全更新富丽系统文件
5. **新增自更新基础设施**：`VERSION` + `scripts/update_fuli.py`
6. **安装心智转变**：富丽安装到 Agent 全局，而非项目本地
7. **README 重写**：安装 → 扫描 → 升级 → 卸载 四步清晰
8. **明确两层空间**：富丽升级只动系统文件，`.fuli/` 用户资产不动
9. **标准 frontmatter**：version 字段回到 `version: "0.4.0"`（不再使用 `metadata.version`）
10. **`富丽扫描` 内部自动选择 SCAN/REVIEW/REFLECT**，用户无需记忆三种模式

## 验收标准

```text
第一次：安装富丽 → 富丽扫描
日常：富丽扫描
维护：富丽升级
```

三个动作跑通，富丽从"不错的 Skill 原型"进入"可长期使用的产品"。

## 发布流程

```
根目录开发 → 升级 VERSION → 更新 CHANGELOG → 评测 → commit → git tag v0.4.0 → GitHub Release
```

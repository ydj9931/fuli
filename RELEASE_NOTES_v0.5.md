# 富丽 v0.5 版本说明

## 版本主题

> **Scope & Lifecycle（最小作用域与生命周期）**

v0.3 解决：富丽应该如何判断。
v0.4 解决：用户应该如何自然地使用富丽。
v0.5 解决：富丽应该如何介入、何时退出。

## 为什么这一版只做这一件事

v0.4 之后暴露一个问题：富丽被加载后，没有明确"什么时候退出"。

如果不解决，富丽会逐渐变成无处不在、过度干预的 Agent 人格层。

v0.5 不增加任何能力，只把**调用、作用、退出机制**做扎实。

## 核心变化

1. **最小作用域原则**：富丽只在需要"复利创作判断"的最小对象、最短任务、最小上下文中生效
2. **瞬时顾问定位**：富丽是宿主 Agent 之上的增益层，不是另一个常驻 Agent
3. **生命周期协议**：识别最小作用域 → 加载必要原则 → 执行 → 返回 → 立即退出
4. **三种生命周期**：一次性动作（默认 95%）/ 项目动作 / 显式持续模式
5. **完成即退出（最高优先级）**：指令完成、对象改变、无关任务、调用其他 Skill、用户喊停，任一触发即退出
6. **最小上下文读取**：渐进展开，能用 1 个文件判断就不读 10 个
7. **两状态模型**：`inactive` / `active(task)`，不做复杂状态机
8. **新增原则文档**：`references/minimal-scope.md`

## 设计原则

最小认知负载 + 最小行动负载 + 最小作用域：

> **知道得刚好，做得刚好，介入得刚好。**

## 验收标准

```text
富丽扫描 → 输出建议 → 富丽自动退出
用户说"帮我写封邮件" → Agent 正常完成，不套用富丽框架
```

## 发布流程

```
根目录开发 → 升级 VERSION → 更新 CHANGELOG → 评测 → commit → git tag v0.5.0 → GitHub Release
```
